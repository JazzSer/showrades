# Showrades — Prioritised Backlog & Project Status

**Version:** 0.3
**Last Updated:** 2026-06-14
**Status:** 🟢 P0 Core Game Loop Built — Pending Manual Playtest

---

## Current Status (Architecture Layers)

| Layer | Status | Notes |
|---|---|---|
| Brand guide | ✅ Done | HTML brand guide in `/docs/designs/` |
| Tech stack scaffold | ✅ Done | Next.js 16 App Router + Tailwind v4 + Supabase |
| Design tokens | ✅ Done | All CSS vars + Tailwind utilities in `globals.css` |
| Fonts | ✅ Done | Fredoka (display) + Nunito (body) via next/font |
| Component library | ✅ Done | Button, Chip, GameCard, Timer, TeamCard, MimoMascot, Logo |
| Supabase client | ✅ Done | `utils/supabase/{client,server}.ts` (browser + server helpers), env vars filled. No middleware — see Decision History |
| Game screens | ✅ Done | Home, Setup, Categories, Game (pass + play), Round Summary, Game Over |
| Game logic | ✅ Done | `useGameState` context + `useCountdown` hook, localStorage-persisted |
| Content (words/images) | ✅ Done (emoji-based) | 98 word cards across 6 categories in `lib/data/words.ts` |
| Deployment | ❌ Not started | — |
| User testing | ❌ Not started | — |

---

## Backlog

Task board, organised by feature/epic, Agile style. **Assignee** is left blank for subagents — fill it in when a subagent picks up a task (e.g. `frontend-dev`, `content-curator`).

**Status legend (prefix on Task):** ⬜ To Do · ▶️ In Progress · 🌠 In Review / Feedback Pending · ✅ Done · 🚫 Blocked
**Priority:** `P0` Core Game Loop · `P1` Launch Quality · `P2` Post-MVP Polish · `P3` Logged-In Features · `Ice Box` Future Exploration
**Latest Status:** free-text remarks for anything not ⬜/✅ — subagents and Jazz log progress, blockers, or review notes here.

### EPIC: Core Game Loop (MVP Build)

| ID | Task | Description | Acceptance Criteria | Priority | Latest Status | Assignee |
|---|---|---|---|---|---|---|
| SR-001 | ✅ Design Home Screen | Logo, tagline, "Start Game" CTA, "How to Play" rules toggle | Implemented in `app/page.tsx`: Logo, tagline, Start Game → `/setup`, How to Play toggle | P0 | — | — |
| SR-002 | ✅ Design Game Setup / Category setup screen | Game Mode toggle (Picture/Word), team management, Game Style toggle (Default/Heads Up) | Implemented in `app/setup/page.tsx`: mode toggle, 2–6 teams add/remove/rename/avatar, Default/Heads Up style | P0 | — | — |
| SR-008 | ✅ Create `useGameState` hook | Central game state context (teams, scores, deck, round, screen) | `hooks/useGameState.tsx` provides Context + Provider, persisted to `localStorage` (`showrades_game_v1`) | P0 | — | — |
| SR-009 | ✅ Create `useCountdown` hook | Round timer hook | `hooks/useCountdown.ts` exposes `current`, `isRunning`, `start`, `pause`, `reset` | P0 | — | — |
| SR-010 | ✅ Create `lib/data/words.ts` | Typed word data + category helper | Exports `WORD_CARDS` and `getCardsForCategories()` | P0 | — | — |
| SR-011 | ✅ Build Home screen | — | `app/page.tsx` renders Logo, tagline, Start Game → `/setup`, How to Play toggle | P0 | — | — |
| SR-012 | ✅ Build Setup screen | — | `app/setup/page.tsx`: mode toggle, team management (2–6 teams), Game Style toggle | P0 | — | — |
| SR-013 | ✅ Build Categories screen | — | `app/categories/page.tsx`: category multi-select with card counts, round timer (30/60/90s), Start Round CTA | P0 | — | — |
| SR-014 | ✅ Build Game screen | — | `app/game/page.tsx`: pass-the-phone + play sub-screens, timer countdown, GameCard/emoji, Got It/Pass, auto-advance on timeout, deck reshuffle on exhaustion | P0 | — | — |
| SR-015 | ✅ Implement scoring logic | — | +1 to active team on Got It, persisted to `localStorage`, team rotation each turn, win at 15 pts | P0 | — | — |
| SR-016 | ✅ Build Round Summary screen | — | `app/game/summary/page.tsx`: scoreboard via TeamCard, leader/tie messaging, Next Round / End Game buttons | P0 | — | — |
| SR-017 | ✅ Build Game Over screen | — | `app/game/end/page.tsx`: winner announcement with Excited Mimo, final scoreboard, Play Again resets to Setup | P0 | — | — |
| SR-050 | ✅ Redefine game round type | Parent task — turn-based ("by card") vs round-based ("by round") gameplay, see SR-050.1–.6 for refined subtasks. | The actual gameplay reflects correct behaviour according to the type of game round | P0 | Done 2026-06-14 — all subtasks SR-050.1–.6 complete, full 8-combination regression matrix passed (see `docs/3. Test Cases.md`). | — |
| SR-050.1 | ✅ Fix score attribution in card mode | In "by card" (turn-based) mode, the team that correctly guesses ("Got It") should score the point, not the acting team that currently gets credited in `onCorrect()` | In a 2-team card-mode game, the team that correctly guesses the word receives the point, verified across multiple turn alternations | P0 | Done — `onCorrect()` already credits the guessing team in card mode; spot-checked across 4 turn alternations in-browser 2026-06-14. | frontend-dev |
| SR-050.2 | ✅ Skip round summary in card mode | In "by card" mode, games should play as one continuous game — no "Round Complete" interruption every full team rotation; only show summary screens at win-condition or deck exhaustion (gameover) | In a 2-team card-mode game with `goalMode: "points"`, the round-summary screen does not appear between turns, only `gameover` appears on reaching `winTarget` | P0 | Done — verified `screen` never becomes `"roundend"` in card mode across multiple turns; only `pass`/`play`/`gameover` occur. | frontend-dev |
| SR-050.3 | ✅ Define card-mode timer behaviour | Card mode has no timer by default (card ends only on Pass/Got It). If the round-length timer is configured in setup, each card gets that length as a per-card time limit, same as round-based, with clear UI on expiry and correct deck/turn advance | In card mode with no timer configured, cards have unlimited time; with a timer configured, each card counts down from the configured length and expiry correctly advances the deck/turn with visible feedback | P0 | Done 2026-06-14 — fixed pass screen never reappearing after turn 1 (`app/game/page.tsx`, new `goToPass()` helper), which was also why the timer never reset/restarted on cards 2+. Also fixed a Strict-Mode double-invoke bug in `useCountdown.ts` that caused timer expiry to double-advance the deck and skip team rotation. Verified timer resets/counts down on every card and expiry correctly advances deck+turn with "Time's up!" feedback. | frontend-dev |
| SR-050.4 | ✅ Fix "Finish Game" messaging on deck exhaustion in round mode | When the deck is exhausted mid-rotation (not the last team) in round mode, the game should pass to the next team rather than showing "Finish Game" / "Deck complete!" copy | In a 3-team round-mode game with `goalMode: "endless"`, exhausting the deck during Team A's round shows a "pass to next team" prompt, not "Finish Game"; the game only ends after the last team's round | P0 | Done 2026-06-14 — `endTurn()` previously ended the game for ANY team on deck exhaustion in endless mode; now only the last team's exhaustion triggers `gameover`. Non-last teams see a new "Deck complete! ... pass the phone to the next team" prompt with a "Pass to Next Team →" button. Verified with 3-team endless game (16-card deck). | frontend-dev |
| SR-050.5 | ✅ Update pass-screen copy for card-mode role-swap | In card mode, the pass screen should read naturally for a 2-person role-swap (e.g. "Your turn to act!") rather than the multi-person "Give the phone to {team}... Everyone else — no peeking!" framing | In card mode, pass-screen copy reads naturally for a role-swap; round-mode copy is unchanged | P1 | Done 2026-06-14 — copy ("Your turn to act, {team}! Pass the phone — everyone else guesses.") was already correct but the pass screen wasn't reappearing after turn 1 (fixed as part of SR-050.3), so it was never visible. Confirmed it now shows and reads naturally before every card; round-mode copy unchanged. | frontend-dev |
| SR-050.6 | ✅ Regression test matrix for round types | Manual test pass covering card mode (2-team, timer on/off), round mode (3-team, timer on/off), deck exhaustion in both modes for both goal modes, and win-condition triggers in both modes | Documented test pass for all 8+ combinations, with no unexpected `roundend`/`gameover` transitions or score misattributions | P0 | Done 2026-06-14 — all 8 combinations (TC-01–TC-08) tested and passed by product owner. Documented in `docs/3. Test Cases.md`. | Jazz |
| SR-051 | ✅ Incorporate sourced images into Picture mode | Parent task — refined 2026-06-14 into SR-051.1–.2. Scope: Animals category (all 18 words in `lib/data/words.ts`), images linked from Pexels per `docs/content-curation/animals-images.json` | Gameplay shows real photos (not just emoji) for Animals category in Picture mode so Product Owner can review | P0 | Done 2026-06-14 — both subtasks complete and verified in-browser (desktop + 375px). Ready for PO review. | — |
| SR-051.1 | ✅ Resolve/source Animals image URLs | Resolve direct Pexels image URLs for the 14 Animals words already in `animals-images.json` (currently `imageUrl: null`); source new Pexels photos for the 4 missing words (Monkey, Crocodile, Tiger, Horse) | `docs/content-curation/animals-images.json` and `.md` have a resolved direct image URL + license info for all 18 Animals words in `lib/data/words.ts` | P0 | Done — all 18 URLs verified HTTP 200 (3 of the original 14, Crocodile/Snake/Frog, were 404 and replaced with freshly-sourced Pexels photos). | content-curator |
| SR-051.2 | ✅ Render images in Picture mode (Animals) | Add optional `image?: string` to `WordCard`, populate `animals` entries in `lib/data/words.ts` with resolved URLs from SR-051.1, render `card.image` (fallback to emoji) in Picture mode card on `app/game/page.tsx` | Picture mode with Animals category shows real photos for all 18 words, verified at 375px and desktop; emoji-only categories unaffected | P0 | Done — `npx tsc --noEmit` clean, verified in headless browser at 1280px and 375px (Elephant, Giraffe photos render correctly; non-Animals categories still show emoji, e.g. Basketball). | frontend-dev |
| SR-053 | ⬜ Remove redundant "How to Play" button on Home screen | Home screen has both a "How to Play" button/toggle and a "?" button in the top right that does the same thing — remove the redundant bottom button | `app/page.tsx` Home screen has a single "How to Play" entry point (the "?" button); the long bottom button is removed | P2 | Logged 2026-06-14 from product owner feedback. | — |

### EPIC: Content & Word Library

| ID | Task | Description | Acceptance Criteria | Priority | Latest Status | Assignee |
|---|---|---|---|---|---|---|
| SR-003 | ✅ Compile draft word list | 98 entries across the 6 locked categories | `lib/data/words.ts` contains `WordCard[]` (word, category, emoji) for Animals, Food, Fruits, Jobs, Veggies, Sports | P0 | — | — |
| SR-004 | ✅ Research educational words for children | Source material for sneaky-educational content direction | Research compiled in `Research on contents.md`, informs word selection | P0 | — | — |
| SR-005 | ✅ Compile `pictures.json` | Visual asset mapping for Picture Charades | Superseded — emoji field on `WordCard` in `lib/data/words.ts` covers this | P0 | — | — |
| SR-006 | ▶️ Source sound effects | Correct answer / pass / timer sound assets | Sound effect assets identified and sourced | P1 | Running in claude/trusting-hypatia-kd4zdr | https://claude.ai/code/session_01LJzpN5M8Xbe2mQmYMAwZPq |
| SR-036 | ⬜ Expanded content packs | Additional categories beyond the locked 6 (Vehicles, Everyday Stuff, Nature, TV & Movies, Actions) | New category packs added to `lib/data/words.ts`, sourced from `Research on contents.md` | P2 | — | — |
| SR-037 | ⬜ Image mode parent hint ("Secret Clue") | Discreet hint text below image, tap-to-reveal, visible only to the holding parent | Tap-to-reveal "Secret Clue" text appears below image in Picture mode, per worked examples in `Research on contents.md` | P2 | — | — |
| SR-038 | ⬜ Educational Twist | One-line educational fact per word, surfaced on Round Summary screen | Round Summary displays a relevant educational fact per word played, per `Research on contents.md` and `1. Vision & Goals.md` §5 | P2 | — | — |

### EPIC: Responsive, Accessibility & Mobile Polish

| ID | Task | Description | Acceptance Criteria | Priority | Latest Status | Assignee |
|---|---|---|---|---|---|---|
| SR-018 | ▶️ Responsive layout pass | Test and fix all screens at 375px, 768px, 1024px+ | All screens render with no overflow/clipping at 375px, 768px, 1024px+ on iOS Safari + Android Chrome | P0 | Recent commits ("reworking the screens", "Fix mobile layout") suggest this is underway — verify against current screens before re-scoping. | — |
| SR-019 | ⬜ Clean up page classnames | Explore making classnames human-readable, reusable, still customisable | Page classnames follow a consistent, documented convention without sacrificing Tailwind customisation | P0 | — | — |
| SR-021 | ⬜ Touch target audit | All interactive elements ≥44×44px (WCAG 2.1) | Every button/interactive element across all screens measures ≥44×44px on mobile | P0 | — | — |
| SR-023 | ⬜ Accessibility baseline | Alt text on images, ARIA labels on icon-only buttons, keyboard navigation, visible focus indicators | All Picture Charades images have alt text; icon-only buttons (Got It, Pass, timer) have ARIA labels; all interactive elements are keyboard-reachable with visible focus rings | P2 | — | — |

### EPIC: Feedback, Animation & Audio

| ID | Task | Description | Acceptance Criteria | Priority | Latest Status | Assignee |
|---|---|---|---|---|---|---|
| SR-020 | ⬜ Wire Mimo animations to game events | Got It → Excited Mimo + `animate-pop-in`; Pass/Timeout → Sad Mimo + `animate-wiggle`; Timer ticking → Thinking Mimo; Home idle → Happy Mimo + `animate-bounce-spring` | Mimo mascot state and animation class change correctly for each listed game event | P0 | — | — |
| SR-033 | ⬜ Sound effects | Correct answer chime, timer tick, timer-end buzzer, opt-in toggle | Sound effects play for Got It, timer tick, and timer-end; toggleable in settings | P2 | — | — |
| SR-052 | ⬜ Team-colour theming on gameplay screens | Parent task — feedback from SR-050.6 playtest (2026-06-14). Use each team's assigned colour as the background/accent on the pass screen and the active-play screen, so kids/elderly can recognise whose turn it is at a glance. See SR-052.1–.2. | The pass-the-phone and active-play screens are visually colour-coded per team, distinct from each other | P1 | Logged 2026-06-14 from product owner playtest feedback. | — |
| SR-052.1 | ⬜ Team-colour background on pass/transition screen | The "We're Ready →" pass screen background should reflect the active team's colour (from `TEAM_COLORS`/`AVATAR_BG`) instead of the current fixed plum background | `app/game/page.tsx` pass screen background uses the current team's colour | P1 | — | — |
| SR-052.2 | ⬜ Team-colour background on active-play screen | The play screen background should reflect the active team's colour instead of the current fixed sky/sun (mode-based) background | `app/game/page.tsx` play screen background uses the current team's colour while preserving mode-specific card styling | P1 | — | — |
| SR-054 | ⬜ Mute/unmute toggle for sound & music | Once SR-033/sound effects + music are added, provide a persistent mute/unmute button accessible from any screen | A mute/unmute control is reachable from every screen and toggles all game audio | P2 | Logged 2026-06-14 from product owner feedback. **Design first**: hand to designer for placement/visual treatment before implementation; blocked on SR-033 (sound effects) landing. | — |

### EPIC: Offline Support

| ID | Task | Description | Acceptance Criteria | Priority | Latest Status | Assignee |
|---|---|---|---|---|---|---|
| SR-022 | ⬜ Service Worker (offline support) | Minimal Workbox config so the game runs after first load | Game is fully playable offline after first visit, via a Workbox-registered service worker | P0 | — | — |

### EPIC: Deployment & CI/CD

| ID | Task | Description | Acceptance Criteria | Priority | Latest Status | Assignee |
|---|---|---|---|---|---|---|
| SR-025 | ✅ Choose hosting | — | GitHub Pages selected (branch deploy for testing, workflow deploy post-launch) | P1 | — | — |
| SR-026 | ✅ Set up GitHub repository | — | Repo created, `main` branch pushed (not yet branch-protected) | P1 | — | — |
| SR-027 | ⬜ Configure CI/CD | GitHub Actions pipeline: lint → type-check → build → deploy, auto-deploy on push to `staging` | Pushing to `staging` triggers a pipeline that lints, type-checks, builds, and deploys; failures block deploy | P0 | — | — |
| SR-028 | ⬜ Add production environment variables | Supabase URL + anon key in hosting dashboard | Production deployment has Supabase URL + anon key configured and working | P3 | — | — |

### EPIC: Documentation

| ID | Task | Description | Acceptance Criteria | Priority | Latest Status | Assignee |
|---|---|---|---|---|---|---|
| SR-029 | ⬜ Write README.md | Project overview, local dev setup, deployment guide, how to add content | README covers: what the project is, how to run locally, how to deploy, how to add new words/categories | P1 | — | — |
| SR-030 | ⬜ Write content contribution guide | How to add new words/images without touching code | Guide documents the process for adding/editing words and categories in `lib/data/words.ts` for non-developers | P1 | — | — |

### EPIC: Gameplay Modes & Settings

| ID | Task | Description | Acceptance Criteria | Priority | Latest Status | Assignee |
|---|---|---|---|---|---|---|
| SR-031 | ✅ Configurable timer | 30s/60s toggle in lobby (Open Question #2) | Already implemented — 30/60/90s on Categories screen | P2 | — | — |
| SR-032 | ✅ More than 2 teams | Up to 4+ teams with custom names + colour/emoji assignment | Already implemented — Setup screen supports 2–6 teams with custom names + avatars (SR-002). Confirm no further polish needed | P2 | — | — |
|SR- 049 |⬜ Combine Game Setup, Categories Page into 1 page |All of the existing settings in both pages should wind up as one singular, scrollable page |P1|---|---|---|
| SR-034 | ⬜ "Heads Up" style mode | Phone tilted → next card; tilt back → pass (DeviceOrientation API) | Tilting phone forward advances to next card; tilting back registers Pass, using DeviceOrientation API | P2 | — | — |
| SR-035 | ✅ Category selection per-player vs per-game | Open Question #8 | Resolved — per-game selection on Categories screen confirmed as final approach; no per-player mode planned | P2 | — | — |
| SR-055 | ⬜ [Explore] Card-mode scoring with 3+ teams | Card mode (SR-050) currently assumes a 2-team role-swap: the team NOT holding the phone scores the point (`onCorrect` credits `currentTeamIndex + 1`). With 3+ teams it's ambiguous who "got it" — investigate whether card mode should be capped at 2 teams, or define a mechanic for tracking which of 3+ teams guessed correctly | Written recommendation: either (a) restrict "By card" turn mode to 2 teams in Setup, with rationale, or (b) a defined UX for selecting the scoring team among 3+ in card mode, before any implementation | P1 | Logged 2026-06-14 from product owner feedback during SR-050.6 playtest. Exploration/decision needed before further card-mode work. | — |

### EPIC: Analytics

| ID | Task | Description | Acceptance Criteria | Priority | Latest Status | Assignee |
|---|---|---|---|---|---|---|
| SR-039 | ⬜ Analytics | Find a GitHub-Pages-native analytics solution | Lightweight analytics integrated and working on static GitHub Pages hosting | P2 | — | — |

### EPIC: Accounts & Cloud (Supabase Auth)

| ID | Task | Description | Acceptance Criteria | Priority | Latest Status | Assignee |
|---|---|---|---|---|---|---|
| SR-007 | ✅ Set up Supabase project | Create project, copy URL + anon key into `.env.local` | Supabase project created; `.env.local` populated with URL + anon key | P0 | — | — |
| SR-040 | 🚫 User accounts | Sign up / sign in via Supabase Auth (email or Google OAuth) | Users can create an account and log in/out via email or Google OAuth | P3 | Blocked until P0–P1 are complete. | — |
| SR-041 | 🚫 Custom word decks | Logged-in users can create, save, and share their own word packs | Logged-in user can create, edit, save, and delete a custom word deck | P3 | Blocked until P0–P1 are complete. | — |
| SR-042 | 🚫 Deck sharing | Public deck URLs; other users can fork a shared deck | A custom deck has a shareable public URL; another user can fork it into their own account | P3 | Blocked until P0–P1 are complete. | — |
| SR-043 | 🚫 Play history | Save past game scores per account | Logged-in user can view a list of past games with scores/dates | P3 | Blocked until P0–P1 are complete. | — |

### EPIC: Future Game Modes & Platforms (Ice Box)

| ID | Task | Description | Acceptance Criteria | Priority | Latest Status | Assignee |
|---|---|---|---|---|---|---|
| SR-044 | ⬜ "Who Am I?" mode | Sticky-note-on-forehead variant (famous person / character guessing) | New game mode where players guess a famous person/character assigned to them | Ice Box | — | — |
| SR-045 | ⬜ Multi-language word decks | Spanish, French, Mandarin, etc. | Word decks available in at least one additional language, selectable in Setup | Ice Box | — | — |
| SR-046 | ⬜ Multiplayer / networked play | Shared session where each device is a separate team's screen (Supabase Realtime) | Multiple devices join a shared session via Realtime, each controlling one team's screen | Ice Box | — | — |
| SR-047 | ⬜ Teacher/classroom mode | Curriculum-aligned word packs, class code to join a session | Teacher creates a session with a class code; students join with curriculum-aligned word packs | Ice Box | — | — |
| SR-048 | ⬜ Native app wrapper | Capacitor/Expo shell for App Store / Google Play listing | App builds and runs as a native wrapper, installable via App Store / Google Play | Ice Box | — | — |

---

## Testing Milestones

| Milestone | What to Test | Who | When |
|---|---|---|---|
| **Internal smoke test** | All game flows end-to-end on iOS Safari + Android Chrome | Developer (Jazz) | After P0 complete |
| **Device matrix test** | iPhone SE, iPad, Android mid-range, desktop Chrome | Developer | Before P1 deploy |
| **Lighthouse audit** | Performance ≥ 90, Accessibility ≥ 90, Best Practices ≥ 90 | Developer | Before launch |
| **Family playtest (round 1)** | 1 adult + 1 child (age 5–7) playing Picture Charades together | Real users | Immediately after first deploy |
| **Family playtest (round 2)** | Mixed-age group (grandparent + tweens + adults) playing Word Charades | Real users | 1–2 weeks post-launch |
| **Stress test** | Exhaust full word/image deck, rapid tapping, offline play after load | Developer | After Service Worker added |
| **Accessibility audit** | Screen reader (VoiceOver/TalkBack), keyboard-only navigation | Developer + a11y tool | Before P1 complete |

---

## Open Questions (from PRD)

| # | Question | Priority |
|---|---|---|
| 1 | Final app name confirmed as "Showrades"? | ✅ Confirmed |
| 2 | Timer: fixed 30s or configurable? | ✅ Resolved — configurable 30/60/90s, set on Categories screen |
| 3 | Reveal mechanic: hide word until actor is ready? | ✅ Resolved — no hide; actor sees card directly |
| 4 | Illustration source for Picture Charades? (OpenMoji / unDraw / original) | ✅ Resolved — emoji-based for MVP, custom illustrations deferred |
| 5 | Scoring for more than 2 teams? | ✅ Resolved — supports 2–6 teams, win at 15 pts, built in P0 |
| 6 | Sound effects? | P2 — see SR-033 |
| 7 | Service Worker truly needed for MVP? | P0 — see SR-022 |
| 8 | Category selection per-player or per-game? | ✅ Resolved — per-game, on Categories screen |

---

## Decision History

### 2026-06-11 — P0 Build Decisions Log

A working prototype (`docs/designs/Showrades Prototype.html`) was found to already implement a full game loop with rules that diverged from this backlog/PRD. Per product owner decision, **this backlog/PRD remains the source of truth** where they conflict, but the prototype's game rules were reviewed and explicitly locked in below. The following decisions resolve the P0 open items and were used to build the current `app/`, `hooks/`, and `lib/` code:

- **Architecture:** Next.js multi-route App Router (`/`, `/setup`, `/categories`, `/game`, `/game/summary`, `/game/end`) with a shared `GameProvider` React Context (`hooks/useGameState.tsx`) persisted to `localStorage` (`showrades_game_v1`) — chosen over a single-page state machine for cleaner separation of screens and easier future extension.
- **Reveal mechanic (Q3):** No hide/flip step. The actor sees the word/emoji directly and holds the phone away from teammates. Resolves "Decide on reveal mechanic."
- **Timer (Q2):** Configurable per round — 30 / 60 / 90s, selected on the Categories screen. Resolves "Decide timer duration."
- **Categories (Q4 / "Finalise category list"):** Locked at 6 — Animals (mint), Food (sun), Fruits (coral), Jobs (sky), Veggies (plum), Sports (sky). Supersedes the original 5-category MVP list (Occupations → Jobs, added Sports).
- **Picture Charades visuals:** Emoji-based for now (no SVG illustration pipeline). Each word card carries an `emoji` field; Picture mode renders large emoji + word. Custom illustrations remain a future option.
- **Game rules locked in:** Win at 15 points (`WIN_TARGET`), "Got It" = +1, "Pass" = 0, multi-round play with shuffled/reshuffling deck, 2–6 teams supported.
- **"Heads Up" style (P2 item):** Implemented now as a label-only toggle ("HEADS UP" vs "YOUR TURN") — no device-tilt/DeviceOrientation logic. The full tilt-based interaction remains in P2 if desired.
- **Category selection (Q8):** Per-game (selected once on the Categories screen before starting), not per-player.

**Known issue fixed:** `app/game/page.tsx` had an edge case where navigating to `/game` with a fresh/default game state (`screen: "home"`) could leave the user stuck on the "pass the phone" screen after tapping "ready". Fixed by simplifying the play/pass screen condition.

**Outstanding from this build:** Interactive browser verification (timer countdown, button taps, multi-round flow) has not been performed — no browser automation tooling is available in the dev environment. All routes verified to return HTTP 200 with expected content via SSR, and `npx tsc --noEmit` passes with zero errors. Recommend a manual playtest pass on `http://localhost:3100` (or your dev port) covering: Setup → Categories → full round → Round Summary → Next Round → Game Over → Play Again.

### 2026-06-13 — Hosting / Supabase Middleware Decision

The app deploys as a static export (`output: "export"` in `next.config.ts`, to GitHub Pages), which Next.js does not allow combined with middleware (`⨯ Middleware cannot be used with "output: export"`). A Supabase SSR middleware (`middleware.ts` + `utils/supabase/middleware.ts`) was briefly added and has been **removed** per product owner decision:

- **Decision:** Stay on static export / GitHub Pages for now. No middleware-based session refresh.
- **Why:** P3 (accounts, custom decks, sharing, play history, multiplayer) is the only backlog area needing Supabase, and none of those require SSR — they're all achievable with the browser Supabase client (`utils/supabase/client.ts`) + RLS policies, with sessions persisted client-side (same pattern as `useGameState`'s localStorage persistence). Migrating hosting now would be solving a problem the project doesn't have yet, at the cost of a new deploy pipeline.
- **Kept:** `utils/supabase/client.ts` (browser client, ready for P3 auth work) and `utils/supabase/server.ts` (harmless, ready if a future SSR need — e.g. a server-rendered public deck-sharing page — justifies revisiting hosting).
- **Revisit if:** a feature genuinely requires server-rendered per-user pages or Next.js API routes. That would be a deliberate, isolated hosting migration decision at that time.

---

*End of Backlog v0.3*
